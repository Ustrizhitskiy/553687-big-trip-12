import {SortType} from "../const";

export const sortItems = [
  {
    type: SortType.EVENT,
    name: SortType.EVENT.toUpperCase()
  },
  {
    type: SortType.TIME,
    name: SortType.TIME.toUpperCase()
  },
  {
    type: SortType.PRICE,
    name: SortType.PRICE.toUpperCase()
  }
];
